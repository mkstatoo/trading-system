version: '3.9'

services:
  bot:
    build: .
    container_name: trading_bot
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./trading.db:/app/trading.db
